import {Usuario} from '../../general/model/usuario';

export class User {
  username: string;
  email: string;
  // tslint:disable-next-line:variable-name
  access_token: string;
  usuario: Usuario;
}
